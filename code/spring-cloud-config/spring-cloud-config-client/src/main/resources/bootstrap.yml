management:  endpoints:    web:      exposure:        include: refresh,metrics,beans,bus-refresh        


spring:  
  application:
    name: springcloudconfigclient
  cloud:
    config:
      uri: http://localhost:4000
      fail-fast: true#  rabbitmq:#    host: localhost#    port: 5672#    username: guest#    password: guest